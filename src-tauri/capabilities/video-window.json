{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "video-window",
  "description": "The Comparison video pop-out. It only needs to receive seek commands, read its own window, and go full screen — deliberately nothing else: no filesystem, no shell, no window creation.",
  "windows": [
    "mark-video"
  ],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-emit",
    "core:window:allow-close",
    "core:window:allow-minimize",
    "core:window:allow-maximize",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging"
  ]
}
